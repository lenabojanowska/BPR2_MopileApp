<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobileapp" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileApp"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".activities.profile.ProfileActivity"
            android:exported="false"
            android:label="ProfileActivity"
            android:theme="@style/Theme.MobileApp.NoActionBar" />
        <activity
            android:name=".activities.basket.BasketActivity"
            android:exported="false"
            android:label="BasketActivity"
            android:theme="@style/Theme.MobileApp.NoActionBar" />
        <activity
            android:name=".activities.search.SearchActivity"
            android:exported="false"
            android:label="SearchActivity"
            android:theme="@style/Theme.MobileApp.NoActionBar" />
        <activity
            android:name=".activities.wishlist.WishlistsActivity"
            android:exported="false"
            android:label="WishlistActivity"
            android:theme="@style/Theme.MobileApp.NoActionBar" />
        <activity
            android:name=".activities.scan.ScanActivity"
            android:exported="false"
            android:label="ScanActivity"
            android:theme="@style/Theme.MobileApp.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="MainActivity"
            android:theme="@style/Theme.MobileApp.NoActionBar" />
        <activity
            android:name=".activities.firebase.LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.MobileApp.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>